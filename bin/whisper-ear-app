#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WHISPER_EAR_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$WHISPER_EAR_DIR"
exec "$HOME/miniforge3/bin/python3" "$WHISPER_EAR_DIR/whisper_ear_app.py" "$@"
